# GitHub Pages configuration
# Disable Jekyll processing for static HTML files
skip_jekyll: true

# Exclude files from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - .github
  - .git
